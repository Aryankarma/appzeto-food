services:
  - type: web
    name: appzeto-food-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

